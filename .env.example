# Copy to .env.local and adjust.

NEXT_PUBLIC_SITE_NAME=Creston's Food Junction
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Where reservations are delivered. Wire to a Slack/Zapier/CRM webhook,
# or leave empty in development (they log server-side).
RESERVATION_WEBHOOK_URL=
